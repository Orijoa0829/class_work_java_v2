package src.bubble.test05;

/**
 * enum 타입은 왜  쓰는가 ?

 * 도메인 (범위)을 나타낼때

 * ---> 스스로 어떠한 변수의 범위를 정하고 싶을때 !

 * enum을 사용하는 방법
 * 대문자 사용 권장 (내부적으로 상수로 취급함)
 */
public enum PlayerWay {
    LEFT, RIGHT
}
